# AI 学习数据导出目录

浏览器“AI自学习”模块点击“下载学习数据”后，会生成一个 JSON 文件。

建议把下载得到的文件放在本目录中，后续可以直接把这些 JSON 提供给 Codex，用于继续分析和改进 AI：

- 每轮胜者、结束原因、步数、耗时。
- 自动调参前后的评分。
- 每轮行棋记录。
- 当时使用的 AI 参数画像。

浏览器出于安全限制不能直接写入项目目录，因此需要手动把下载文件放进这里。
